export enum PostType {
  idea = 'idea',
  issue = 'issue',
}

export enum Privacy {
  public = 'public',
  private = 'private',
}

export enum Visibleness {
  everyone = 'everyone',
  follow = 'follow',
  approval = 'approval',
  none = 'none',
}
